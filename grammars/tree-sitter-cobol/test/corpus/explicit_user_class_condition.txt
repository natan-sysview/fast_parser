====================================
explicit is not user class
====================================
       identification division.
       program-id. a.
       procedure division.
           IF M57ENTII IS NOT W-DATOS-NUMERI
              CONTINUE
           END-IF.
---

(start
  (program_definition
    (identification_division
      (program_name))
    (procedure_division
      (if_header
        condition: (expr
          (is_not_user_class
            x: (qualified_word
              (WORD))
            tail: (is_not_user_class_tail))))
      (continue_statement)
      (END_IF)
      (period))))
